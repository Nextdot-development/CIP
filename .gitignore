# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
dist-ssr/
build/
.next/
out/
*.tsbuildinfo

# Environment and secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
*.p12
secrets/
.supabase/

# Local database state
*.sqlite
*.sqlite3
pgdata/
.pgdata/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test and coverage output
coverage/
.nyc_output/

# Caches
.cache/
.turbo/
.eslintcache
node_modules/.tmp/

# Editors and OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*.local

# Local Drive object store (development)
.storage/

# Client brand collateral. Real creative assets belong in the product, not in
# a public repository.
tests/fwmagicmomentsjamunbrandcollaterals/
*.collaterals/

# Design references (prototype exports). Kept locally, never committed.
design/

# Client source material. Radico's own reports and decks are working input,
# not source code, and this repository is public.
Radico/

# Training data exported from the database. It is the customer's own
# documents in plain text, and belongs on a disk, not in a repository.
data/
